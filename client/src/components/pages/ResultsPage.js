import React from 'react';

class ResultsPage extends React.Component {
    render() {
        return (
            <div>
                This is the results page where the user can see their results
            </div>
        );
    };
};

export default ResultsPage;